Notebook about SVM and k-means clustering.

For the jupyter notebook to run smoothly 3dshapes image directories should be in the same parent directory with jupyter notebook, 
and cells in the notebook should be run in the order they appear.